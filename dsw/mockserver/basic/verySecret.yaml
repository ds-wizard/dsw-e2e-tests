---
  request:
    method: GET
    path: /verySecret
  response:
    statusCode: 200
    headers:
      Content-Type:
      - application/json
    body: >
      [
        { "id": "secret01", "name": "First secret item" },
        { "id": "secret02", "name": "Second and even more secret item" }
      ]
